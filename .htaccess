RewriteEngine On
RewriteBase /

# Ignore requests for index.html

RewriteRule ^index\.html$ - [L]

# Check if the requested file or directory exists

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
